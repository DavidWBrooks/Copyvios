<Window x:Class="Copyvios.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Copyvios"
        mc:Ignorable="d"
        FontSize="14"
        FocusManager.FocusedElement="{Binding ElementName=articleTitle}"
        SizeToContent="Height"
        Title="Copyvios" Loaded="WindowLoaded">
    <StackPanel HorizontalAlignment="Left" Margin="0" VerticalAlignment="Top" >
        <StackPanel.Resources>
            <Style TargetType="Label">
                <Setter Property="Margin" Value="2"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Margin" Value="2"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="2"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style TargetType="RichTextBox">
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="IsReadOnly" Value="true"/>
                <Setter Property="BorderBrush" Value="Black"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="ClipToBounds" Value="True"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalScrollBarVisibility" Value="Visible"/>
            </Style>
        </StackPanel.Resources>
        <Grid Margin="0,0,0,4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Article title:"/>
            <TextBox x:Name="articleTitle" HorizontalAlignment="Left" TextWrapping="Wrap" Width="240" Grid.Column="1" TabIndex="1"/>
            <Button Content="Compare" Grid.Column="2" HorizontalAlignment="Center" IsDefault="True" Click="StartComparison" Margin="6,2" />
            <Label x:Name="Progress" Grid.Column="3" />
            <Label Content="EB1911 article, or URL:" HorizontalAlignment="Left" Grid.Row="1"/>
            <TextBox x:Name="URL" Grid.Column="1" Grid.Row="1" TextWrapping="Wrap" Grid.ColumnSpan="3" TabIndex="2"/>
        </Grid>
        <Grid Name="ViewGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <RichTextBox x:Name="WPViewer" Height="600">
                <FlowDocument>
                    <Paragraph x:Name="WPPara">
                        <Run Text="The WP text will appear here"/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
            <RichTextBox x:Name="EBViewer" Grid.Column="1" Height="600">
                <FlowDocument>
                    <Paragraph x:Name="EBPara">
                        <Run Text="The EB text will appear here"/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
        </Grid>
    </StackPanel>
</Window>
